language: python
python:
  - "3.3"
  - "3.4"

before_install:
  - sudo apt-get update
  - sudo apt-get install libhdf5-serial-dev

install:
  - pip install .

# command to run tests, e.g. python setup.py test
script:
  - export HOME=`pwd`
  - nosetests -v
